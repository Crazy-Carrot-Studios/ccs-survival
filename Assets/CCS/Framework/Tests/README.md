# CCS Framework — Tests

Automated tests for framework Core and future module assemblies.

- `Runtime/` — play mode and runtime unit tests (future test assemblies)
- `Editor/` — edit mode and editor tests

Runtime smoke test sources live in `Assets/CCS/Framework/Core/Runtime/SmokeTests/` (compiled with `CCS.Core.Runtime`).

Test assemblies will be added in a later milestone. Do not place production code here.
