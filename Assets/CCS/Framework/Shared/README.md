# CCS Framework — Shared

Framework-level shared assets used across modules and projects: art, materials, textures, fonts, audio, and prefabs.

Game-specific content lives under `Assets/CCS/Shared/` at the project root, not here. This folder holds reusable framework resources only.
